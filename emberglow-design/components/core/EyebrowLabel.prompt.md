The signature uppercase letterspaced label that tops every header stack.

```jsx
<EyebrowLabel>Quest in progress</EyebrowLabel>
<EyebrowLabel tone="muted">06 · Active quest</EyebrowLabel>
```

Tones: `ember` (state), `warm` (values), `muted` (footers).
