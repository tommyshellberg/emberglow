Round icon-only button; pass a Lucide SVG as child and always set `label`.

```jsx
<IconButton label="Settings"><i data-lucide="settings" /></IconButton>
<IconButton label="Timer" active><i data-lucide="hourglass" /></IconButton>
```

44px default (minimum hit target). `active` adds a warm Sandy tint + border.
